# Testing the Project

To execute the Busted tests, you don't need to check for luajit, lua, busted, resty, etcetera.

Just make sure Docker daemon is running, and execute:

`./scripts/test-local.sh`

**ASK THE USER FIRST IF THEY WANT TO RUN THIS SCRIPT!** It isn't descructive, just that some corp installation fo coding agents don't like this.
